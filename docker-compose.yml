services:
  faceid:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./chain:/app/chain
      - ./results:/app/results
    restart: unless-stopped
